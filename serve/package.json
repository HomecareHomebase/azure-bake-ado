{
  "name": "Azure-Bake-Serve",
  "version": "0.1.17",
  "description": "A plugin to use the Bake IaC deployment system for Azure",
  "repository": "https://github.com/HomecareHomebase/azure-bake-ado",
  "main": "index.js",
  "typings": "index.ts",
  "directories": {
    "lib": "."
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean": "cmd /c powershell.exe -Command { Remove-Item ./dist -recurse -force; Remove-Item ./node_modules -recurse -force } && npm install",
    "compile": "tsc",
    "clean:build": "npm run clean && npm run compile"
  },
  "author": "HomecareHomebase",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^11.13.2",
    "@types/q": "^1.5.2"
  },
  "dependencies": {
    "azure-pipelines-task-lib": "^2.8.0"
  }
}
