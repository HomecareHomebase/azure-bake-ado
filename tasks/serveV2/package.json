{
  "name": "Azure-Bake-Serve",
  "version": "1.0.0",
  "description": "A plugin to use the Bake IaC deployment system for Azure",
  "main": "index.js",
  "repository": {
    "type": "git",
    "url": "https://github.com/HomecareHomebase/azure-bake-ado"
  },
  "scripts": {
    "init": "npm install",
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean": "git clean -fdX",
    "compile": "tsc -b .",
    "clean:build": "npm run clean && npm run init && npm run compile"
  },
  "author": "HomecareHomebase",
  "license": "ISC",
  "dependencies": {
    "azure-pipelines-task-lib": "^4.0.0",
    "@types/node": "^20.0.0"
  },
  "overrides": {
    "minimatch": "^3.1.4",
    "serialize-javascript": "^7.0.5"
  },
  "devDependencies": {
    "@types/chai": "^4.3.0",
    "@types/mocha": "^10.0.0",
    "chai": "^4.4.0",
    "mocha": "^10.0.0",
    "nyc": "^17.0.0",
    "source-map-support": "^0.5.21",
    "ts-node": "^10.9.0",
    "typescript": "^5.4.0"
  }
}
